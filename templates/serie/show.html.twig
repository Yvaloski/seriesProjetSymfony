{% extends 'base.html.twig' %}

{% block title %}{{ serie.name}} - {{ parent() }}{% endblock %}

{% block body %}
    
    <img src="{{ asset('img/backdrops/' ~ serie.backdrop) }}" width="500px">
    <h1>{{ serie.name}} </h1>
    <p>{{ serie.vote }}</p>
    <p>{{ serie.overview }}</p>

{% endblock %}
